import chai from 'chai';
let expect = chai.expect();

import * as _ from '../underscore/'

describe('Underscore library', ()=>{

    describe('identity', ()=>{

      it('Should return', ()=>{
        _.identity
      })
    });
});
